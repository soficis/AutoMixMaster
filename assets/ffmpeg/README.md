# FFmpeg renderer bundle

Place the FFmpeg executable in `assets/ffmpeg/bin/`.

- Windows: `assets/ffmpeg/bin/ffmpeg.exe`
- Linux/macOS: `assets/ffmpeg/bin/ffmpeg`

You can also override discovery with `FFMPEG_BIN=<absolute path to ffmpeg binary>`.

# SoX renderer bundle

Place the SoX executable in `assets/sox/bin/`.

- Windows: `assets/sox/bin/sox.exe`
- Linux/macOS: `assets/sox/bin/sox`

You can also override discovery with `SOX_BIN=<absolute path to sox binary>`.
